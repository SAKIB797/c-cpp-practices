#include <stdio.h>
int sum()
{
    int x, y;
    int sum = x + y;
    return sum;
////////main
int main()
{
    int x, y;
    printf("Value for first number: ");
    scanf("%d", &x);
    printf("Value for second number:");
    scanf("%d", &y);
    printf("%d", s);
    return 0;
}
